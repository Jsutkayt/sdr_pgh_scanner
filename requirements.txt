# Requirements for RadioTranscriber (File-based version)

# Core dependencies
numpy>=1.21.0
openai-whisper>=20231117
torch>=2.0.0
scipy>=1.7.0
PyYAML>=6.0

# Audio file handling
soundfile>=0.12.0
librosa>=0.10.0  # Optional, for additional audio format support

# Original dependencies (if using original script)
webrtcvad>=2.0.10  # Only needed for live streaming version